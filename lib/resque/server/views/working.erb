<% if params[:id] && worker = resque.find_worker(params[:id]) %>
  <h1><%= worker %>'s job</h1>
  
  <table>
    <tr>
      <th>&nbsp;</th>
      <th>Where</th>
      <th>Queue</th>
      <th>Started</th>      
      <th>Class</th>
      <th>Args</th>
    </tr>
      <tr>
        <td><img src="/working.png" alt="working" title="working"></td>
        <% host, pid, _ = worker.to_s.split(':') %>
        <td><a href="/workers/<%= worker %>"><%= host %>:<%= pid %></a></td>
        <% data = worker.processing %>
        <% queue = data['queue'] %>
        <td><a class="queue" href="/queues/<%= queue %>"><%= queue %></a></td>
        <td><span class="time"><%= data['run_at'] %></span></td>        
        <td>      
          <code><%= data['payload']['class'] %></code>
        </td>
        <td><%= h(data['payload']['args']).inspect %></td>
      </tr>
  </table>

<% else %>

  <h1>Workers Working</h1>
  <table>
    <tr>
      <th>&nbsp;</th>
      <th>Where</th>
      <th>Queues</th>        
      <th>Processing</th>    
    </tr>
    <% if resque.working.empty? %>
    <tr>
      <td colspan="4">Not a one.</td>
    </tr>
    <% end %>
    
    <% for worker_id in resque.working %>
      <% next unless worker = resque.find_worker(worker_id) %>
      <tr>
        <td><img src="/<%= state = worker.state %>.png" alt="<%= state %>" title="<%= state %>"></td>
        <% host, pid, queues = worker.to_s.split(':') %>
        <td><a href="/workers/<%= worker %>"><%= host %>:<%= pid %></a></td>
        <td><%= queues.split(',').map { |q| '<a class="queue" href="/queues/' + q + '">' + q + '</a>'}.join(', ') %></td>
      
        <td>      
          <% data = worker.processing || {} %>
          <% if data['queue'] %>
            <code><%= data['payload']['class'] %></code>
            <small><a class="queue time" href="/working/<%= worker %>"><%= data['run_at'] %></a></small>
          <% else %>
            Waiting for more
          <% end %>
        </td>
      </tr>
    <% end %>
  </table>

<% end %>
<% if params[:id] && (worker = Resque::Worker.find(params[:id])) && (job = worker.job) %>
  <%= partial :worker_show, :worker => worker, :job => job %>
<% else %>
  <%= partial :worker_index %>
<% end %>

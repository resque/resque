<% @subtabs = resque.queues unless partial? || params[:id].nil? %>

<% if queue = params[:id] %>
  <%= partial :queue_show, :queue => queue, :start => params[:start].to_i %>
<% else %>
  <%= partial :queue_index %>
<% end %>
